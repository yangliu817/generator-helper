    <!-- 根据id查询一条记录 -->
    <select id="selectById" parameterType="[primaryKeyType]" resultMap="BaseResultMap">
        select * from [tableName] where [primaryKeyName] = #{[primaryKeyFieldName]}
    </select>
